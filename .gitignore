# data + weights are fetched, never committed (plan §14)
/data/
/weights/
/outputs/

# python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# agent skills installed locally via `skills add` (design guidance, not project code)
/.agents/
/.claude/
/skills-lock.json

# keep the directory structure and any committed fixtures
!/data/.gitkeep
!/weights/.gitkeep
!/outputs/.gitkeep
